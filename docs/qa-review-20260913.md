# Aegis 综合审查与 QA 报告（2026-09-13）

工作方式：4 个并行 Agent + 主线复核。①只读代码审查 ×2（web_app.py / 其余模块+仓库赘余）；②内置浏览器真实用户重度使用 QA ×1（Playwright 真实 Chromium，非 DOM 模拟）；③主线亲自去 GitHub 取高 star 代码简洁 Skill（anthropics 官方 code-simplifier + addyosmani code-simplification）作为修复/简化纪律；④implementer 分两阶段实施，validator 独立验收。审查发现均经主线亲自复核（grep/AST）后才开始改动。

---

## 一、浏览器真实用户 QA 发现（10 bug）

 QA 走了完整真实路径：冷加载→主题切换→高级设置→**9 次真实 LLM 生成**（约 30-40 秒/次出片）→4 Tabs→边界操作（空 prompt/450 字长输入/连点/缩放 80%/125%）。截图存证 `gui-test-screenshots/`（前缀 qa01–qaS5g）。

### P1（明显影响体验）

| # | Bug | 根因 | 状态 |
|---|-----|------|------|
| BUG-1 | 「查看代码」点击后右栏跳到「作品仓库」tab，代码区不展开不定位 | 双 click 处理器冲突（3871 行第二段绑定调 worksTab.click()） | ✅ 已修（F1） |
| BUG-2 | 刷新/重开页面后作品全部丢失，回落地页无历史入口，生成中任务 UI 侧永久丢失 | 「状态即抛式」设计，无持久化 | 📋 报告待定（产品决策） |
| BUG-3 | 生成失败零反馈：UI 静默重置为空占位，无错误提示无诊断ID，用户无法区分失败与没点上 | 失败路径无终态处理 | ✅ 已修（F2） |
| BUG-4 | 模型服务切「OpenAI API」不填 key → 隐藏必填字段静默拦截提交，零请求零提示，跨会话持久，落地页无法自救（近 P0） | hidden+required 的 apiKey 挡表单 | ✅ 已修（F3） |

### P2（打磨项）

| # | Bug | 状态 |
|---|-----|------|
| BUG-5 | 右栏「诊断入口」固定文字溢出面板右边界 | ✅ 已修（F5） |
| BUG-6 | 落地页热门话题缩略图时长徽章叠压标题（"剩余"被遮） | ✅ 已修（F6） |
| BUG-7 | 空 prompt 点生成完全无反馈 | ✅ 已修（F4） |
| BUG-8 | 社区仓库服务 502：warmup ping 数十条 console error；本地模式兜底文案正常 | 📋 报告待定（部署态） |
| BUG-9 | 「同步讲稿」降级提示为英文长串 | ✅ 已修（F7，alignment.py:247 中文化） |
| BUG-10 | 「返回编辑」滚动后输入框被吸顶 header 遮挡 | ✅ 已修（F8） |

### 通过项（都有截图证据）

暗色落地页对比度、主题切换+localStorage 记忆、亮色下动态区域可读性、高级设置 6 项交互、真实生成主流程（含自动兼容修复、视频可播放、代码+元信息）、4 Tabs、450 字长输入、快速连点只发 1 次请求、缩放 80%/125% 无溢出、主题跨会话记忆。

**结构性观察**：BUG-2/3/4 同根因——成功路径反馈完善（步骤条/计时/诊断ID），失败与恢复路径反馈为零。建议后续做「结果落库+历史入口、失败状态框、provider 配置高可达」。

---

## 二、代码审查发现（两轮，均主线复核）

### 轮 1：core/web_app.py（4805 行）

**P0 ×3**：
- P0-1 双重事件绑定（=BUG-1）
- P0-2 `stopProcess()` 死代码，失败/复用/跳过渲染路径进度面板无终态（=BUG-3 根因之一）
- P0-3 审阅 token 走 GET query 且被 `log_message` 明文落盘 `logs/web_runtime.log` → ✅ 已修（F9：改请求头+日志脱敏）

**P1 ×10**（已修 1，其余列入后续建议）：`/instant-svg` 前缀式路径校验缺陷（startswith）；试用入口 UI 可见性与后端能力不一致+死文案；`/api/align` 无门禁无 trial 支持（开放转发面）；`--success/--warn` 未定义（→已修 F10）；`/api/video` 无 Range 不支持 Safari/seek 且整文件读内存；JOB_STORE/VIDEO_CACHE 无淘汰（内存泄漏）；`_read_json_body` 多端点无上限+非 dict body 崩线程；日志追加无锁；`find_latest_video` 兜底 glob 并发可串任务；token 注入页面 JS+非常量时间比较。

**P2 ×17**：死代码（SYSTEM_PROMPT、stopProcess、cloudUnavailable 死分支、永不出现的 spinner）、`.env` 双重加载、响应字段前端不消费、定时文案覆盖真实进度、download 按钮复位缺失、顶层裸 localStorage（隐私模式全页崩）、主题 FOUC、paste 监听无视 vision 开关、render 无超时、日志量过大、断连处理不一致、事件双份存储等。

### 轮 2：其余模块 + 仓库赘余

**P0 ×2（均已修）**：
- `core/manim_agent.py:487` 用 `tempfile` 但没 import → RITL（渲染器在环重试）功能 100% NameError → ✅ 已修（补 import）
- `core/llm_providers.py` 智谱 provider 默认模型是复制粘贴来的 "MiniMax-M3"（模型列表全是 GLM）→ 选智谱不填模型必报错，且预填进 UI → ✅ 已修（glm-4-flash）

**P1 ×8**：api/index.py 死函数内 `scene_name` NameError 地雷（被裸 except 吞成误导性 BAD_GATEWAY）；`"tex" in lowered` 子串误判错误分类（text/context 都命中 latex 分支，修复提示词错向）；截断检测启发式过激（正常赋值结尾/无 wait 场景误判 error 强制重生成）；vision `_extract_json_object` 第二次 loads 未捕获（→已修 H5）；代码生成路径零重试；kimi-code extra_payload 死逻辑；job_id 未消毒拼 URL；网关冷启动重试可挂 90s+。

**死代码（grep+AST 双验证后删除）**：api/index.py `class handler` 整套 149 行（与 app.py ASGI 完全重复）、`generate_manim_code_with_client_provider` 71 行、manim_agent 三个死函数 47 行、llm_providers 恒空函数等。

**仓库赘余清单（只列未删）**：`pyproject.toml.bak`（与正式文件逐字节相同）、`experiment_2026-03-10/`（3.6MB tracked 无引用）、`gui-test-screenshots/`（6.3MB untracked 未进 .gitignore）、过期 `coverage.xml`、根目录 `gen_scene.py`/`test_new_prompt.py`（生成产物入库，后者会被 pytest 误收集）、`demo-pixel-style.html`、上游残留 `my_first_scene.py`/`quickstart_examples.py`。**建议一次性决定去留，故未擅动。**

**scripts/ 引用关系**：4 个零引用上游遗留（make_and_open_docs/template_docsting_with_example/extract_frames/dev_changelog）；其余全部存活（README/tasks/测试引用）。两个 production_*acceptance.py 有 58 行整段重复可提公共模块。

**tests/ 健康**：Aegis 9 个测试质量尚可；但 conftest 顶层 import cairo/moderngl 且失败即 raise → 无 GL 环境连纯逻辑测试都收集不了；pyproject addopts 强制 cov+xdist（实际都在用 `-o addopts=''` 绕过）。

---

## 三、实施的改动

### Phase 0（core 其余模块 + 网关；净删 295 行）

H1 补 `import tempfile`（RITL 修复）；H2 智谱默认模型 glm-4-flash；H3 死代码删除（manim_agent 3 函数、api/index.py handler 类+地雷函数+死分支、llm_providers 恒空函数）；H4 app.py 四段 body 读取抽 helper+重复 Content-Type 消除；H5 vision JSON 解析兜底；H6 course_menu with 语句。
验证：py_compile 全过；Aegis 测试 85 passed / 3 failed 与基线完全一致（3 个失败为 public_trial 已知基线，与本次无关）。

### Phase 1（web_app.py + alignment.py；详见下节验证结论）

修复 F1-F10（QA BUG-1/3/4/5/6/7/9/10 + P0-3 + P1-4）；简化 S1-S9（死 CSS ~200 行、SYSTEM_PROMPT、load_env_file 去重、cloudUnavailable 死分支、download 复位、死 spinner、localStorage 安全 helper、paste 守卫、api/index.py 尾随死常量）。

### Phase 1（web_app.py + alignment.py + api/index.py 尾随死代码）

修复 F1-F10：BUG-1 双绑定、BUG-3/P0-2 失败终态闭环（stopProcess×6 + finishProcess×3 调用点）、BUG-4 隐藏 required 静默死锁（改可见校验+自动展开）、BUG-4/7 空输入可见提示、BUG-5 诊断入口溢出、BUG-6 徽章叠字、BUG-7（并入 F4）、BUG-9 讲稿降级提示中文化、BUG-10 scroll-margin-top、P0-3 审阅 token 改 X-Review-Token 请求头 + log_message token 参数 `***` 脱敏（旧日志有明文实证，新日志实测无）、P1-4 CSS 变量。
简化 S1-S9：死 CSS ~200 行、SYSTEM_PROMPT、load_env_file 去重、cloudUnavailable 死分支、download 按钮复位、死 spinner、safeStorage helper（8 处裸 localStorage）、paste vision 守卫、api/index.py 尾随死常量与死 import。
追加：验收官发现失败后视频占位文案不复位 → stopProcess 增加占位复位（仅当无视频加载时），Playwright 真机验证通过。

### 验证结论（独立验收官 ACCEPT，20/20 PASS）

- 全量 Aegis 套件：**102 passed / 3 failed**（3 个失败为 public_trial 已知基线，与本改动无关，逐名核对一致）
- 页面 200 渲染正常、3 个 script 块 node --check 全过、无 token POST /api/generate 401、元素 id 与路由全部保留、红线零触发
- 浏览器双轮实测（QA Agent 13/13 + 验收官真实 Chrome 全交互 + 主线 Playwright 复核）：主题/落地态/4 Tabs/失败提示/占位复位全部正常

### 总账

| 文件 | 前 → 后 |
|---|---|
| api/index.py | 1645 → 1367（-278） |
| core/web_app.py | 4805 → 4740（-65，功能净增 10 项修复的前提下） |
| core/manim_agent.py | 792 → 739（-53） |
| core/llm_providers.py | 636 → 620（-16） |
| app.py | 189 → 194（+5，抽 helper 后略增） |
| **合计** | **8257 → 7660，净删 403 行（-177/+580）** |

修复清单：P0×5（双绑定、进度面板死代码、token 入日志、RITL tempfile、智谱默认模型）+ P1×3（CSS 变量、vision JSON、Content-Type 重复）+ QA bug×8 + 卫生项×10。

## 四、验收结论

独立验收官（与实施隔离）依据契约逐条执行检查程序，A/B/C/D 共 20 项全部 PASS，最终结论 **ACCEPT**。附注三项（如实登记、不阻塞）：web_app.py 单文件净减 65 行未达 150 行目标（因最大头的同步 generate 去重经主线复核属活路径而正确放弃）；「响应字段瘦身」未逐字进后续建议章节；验收浏览器以 playwright+系统 Chrome 替代 computer-use。

## 五、明确不做 / 后续建议（按优先级）

1. **BUG-2 作品状态持久化**（产品级决策：建议「最近一次结果落 localStorage + 落地页『继续上次』入口」，涉及设计，建议先出交互原型）
2. `/api/align` 补门禁与 trial provider 支持（当前对试用用户必失败，且是开放转发面）
3. `/api/video` 实现 Range/206（Safari 播放+seek+内存）
4. JOB_STORE/VIDEO_CACHE 淘汰 + `/api/generate/start` body 上限 + 日志加锁（长跑稳定性三件套）
5. render 子进程超时 + 前端轮询上限（防永久挂起）
6. 同步 `/api/generate` 与 `run_generate_job` 去重（云端网关+外部 curl 在用，需带真实双端回归的独立验证轮）
7. 试用入口可见性与后端能力一致性（P1-2）+ KIMI/DEEPSEEK 死文案清理
8. precheck 截断检测边界收紧（减少无谓重生成成本）+ `"tex"` 子串误判修复
9. 仓库赘余一次性清理（见第二节清单）+ `gui-test-screenshots/` 进 .gitignore
10. conftest 收集脆弱点：Aegis 测试与上游 GL 依赖解耦


---

# 第二轮：按序解决剩余 bug + 后续建议推进（2026-09-13，用户批准）

依据：第五节后续建议优先级，契约 `docs/acceptance-contract-20260913-round2.md`。实施分两个串行 implementer（WP-A1/WP-A2），主线并行完成 conftest 解耦与仓库清理，最后 Explore 复审 + 浏览器回归 + validator 终验。

## 六、第二轮完成项

| 项 | 内容 | 状态 |
|---|---|---|
| BUG-2/G1 | 作品持久化：lastWork/lastJob 落 localStorage + 落地页「继续上次」入口（夜航风格）+ 恢复视频/代码 + 进行中刷新自动续跑轮询；lastJob 清除按 jobId 守卫（复审修复） | ✅ 真机验证「表现优秀」 |
| BUG-8/G2 | WarmUp ping 指数退避（30s→300s 封顶，成功重置），502 从数十条/会话降到个位数 | ✅ |
| G3 | /api/align 补 X-Aegis-Token 门禁（原裸奔）+ trial provider 支持（本地镜像 run_generate_job，云端 generate_alignment_for_gateway） | ✅ curl 实测 401/400 |
| G4 | /api/video Range/206 分块流式（单区间三形态，非法回落 200），补断连保护；真机 seek 进度条生效 | ✅ 14 项边界单测 |
| G5 | JOB_STORE 数量上限 100 + TTL 2h 惰性清理；VIDEO_CACHE LRU 50；job events 封顶 500 | ✅ |
| G6 | _read_json_body 全部 7 个调用点加上限（generate/align/community 64KB、render 256KB、vision 保留既有 7MB）+ 非 dict → 400 + 默认兜底上限 | ✅ |
| G7 | append_runtime_log/append_bug_log 加 threading.Lock | ✅ |
| G8 | render 子进程超时 600s（AEGIS_RENDER_TIMEOUT 可覆盖）+ 前端 waitForJob 12 分钟总超时 | ✅ |
| G9 | 试用入口仅本地 key 实际可用时展示；KIMI/DEEPSEEK 死文案清理；测试改为显式注入（不依赖 .env） | ✅ |
| G10 | precheck `"tex"` 子串改 `\btex\b` 词边界（text/context 不再误判）；截断检测收紧（waited_after_last_play），F-043 防护保留 | ✅ |
| 建议#9 | 仓库清理：删 experiment_2026-03-10/（3.6MB tracked）、demo-pixel-style.html、my_first_scene.py、quickstart_examples.py、test_new_prompt.py、gen_scene.py（入 .gitignore）、pyproject.toml.bak、过期 coverage 产物 | ✅ |
| 建议#10 | tests/conftest.py GL 解耦：cairo/moderngl 可选导入，GL 上下文不可用时降级 header 不再 raise | ✅ |
| 回归补丁 | 落地页空 prompt 显示「请先输入要讲的概念。」警示（#stageWarn）；favicon 内联 SVG（消除每次冷加载 404） | ✅ 真机验证 |

## 七、复审与终验

**Explore 复审**（全量 diff +805/−1973）抓到 1 个合入阻断项 + 6 个建议项：阻断项是主线恢复 Vercel 入口时用了模块级 `from app import app`，污染 `sys.modules["app"]` 与 render_backend/app.py 模块名冲突，破坏全套 pytest 收集（validator 首验正确 REJECT）。修复为惰性 ASGI 委托（`async def app` 内部延迟导入），import 零污染且 Vercel 入口保留，ASGI 实测 200。另修：空视频 404 边界、videoDuration NaN/Infinity 防护、_read_json_body 默认兜底。复审员的一个删除建议（_extract_download_video_url）经主线复核有测试锁定（test_aegis_public_trial.py:925），保留未删。其余全部确认无误（Range 边界、淘汰逻辑、退避链、f-string、token 安全逐项验证）。

**浏览器回归**（独立 Agent，4 次真实生成）：8 项 7 PASS 1 部分，作品持久化与进行中续跑真机验证通过，0 JS 异常；新发现 3 个低严重度项中 2 个已修（空 prompt 文案、favicon），health 502 轮询为已知部署态。

**validator 终验：ACCEPT**——G1-G10 全 PASS + 硬性门槛全过；Aegis 全套 **113 passed / 3 failed**（3 个为 public_trial 已知基线），冲突组合与全套收集均正常。

## 第二轮总账

改动文件 12 个（web_app.py、api/index.py、app.py、manim_knowledge.py、tests/conftest.py、tests/test_aegis_web_ui.py、.gitignore + 删除 8 个冗余文件/目录）。核心变化：Vercel 入口从 149 行 handler 类换成 8 行惰性 ASGI 委托；本地网关从「align 裸奔 + 无界读取 + 无淘汰」变为「门禁 + 上限 + 三重淘汰 + 双端超时」；用户侧新增作品持久化与恢复能力。

### 仍开放（第三批候选，2026-09-13 已定修复方案，按序执行）

1. ~~**云端 render 上限不一致**~~ **✅ 已修复（2026-09-13，用户拍板 256KB 与本地对齐）**：api/index.py 新增 `RENDER_BODY_MAX_BYTES = 256*1024`，app.py 的 /api/render 路由显式传参。实测约 90KB 请求通过大小门禁到达渲染代理层、300KB 正确 400。背景：真实生成过的最大场景代码 28.6KB，JSON 转义后请求体已贴着旧云端 32KB 上限的边缘，复杂场景存在“本地能过、线上被拒”的风险。
2. ~~**云端 align 鉴权**~~ **✅ 已修复（2026-09-13）**：`AEGIS_CLOUD_REQUIRE_TOKEN=1` 时 app.py 对 POST generate/align/render/vision-analyze 统一校验 X-Aegis-Token（hmac.compare_digest，无配置则 fail-closed）；默认关闭保持既有云端模型。前端 render/vision fetch 已补带 token 头。ASGI 实测：关=零 401；开=无 token 401/有 token 过门禁/GET 类开放。
3. ~~**health 502 轮询**~~ **✅ 已修复（2026-09-13，方案 A）**：web_server.sh 并管 render backend（start/stop/status/restart，AEGIS_WITH_RENDER=0 可关，端口 5001 与 RENDER_BACKEND_URL 对齐，flask 缺依赖时优雅跳过；密钥从 .env 的 RENDER_BACKEND_API_KEY 解析注入 MANIM_API_KEY，根治代理 401/403）。另修 warmup 路由：页面原 ping /api/render/status/health（后端不存在该路由，恒失败），改为新增的 GET /api/render/health（代理后端真实 /health，云端 app.py + vercel.json 同步）。实测 warmup/社区搜索均 200，console 零错误。
4. ~~模块名歧义~~ **✅ 已修复（2026-09-13）**：render_backend/cloud_run_worker.py 改为「包导入优先（from render_backend.app import ...）、裸导入兜底」，新增 render_backend/__init__.py；原始事故场景（根 app.py 占据 sys.modules["app"]）实测免疫；render_backend 内部裸导入按计划保留（start_aegis_local.command 以 CWD=render_backend 裸启动，包导入在该形态不可用）。
